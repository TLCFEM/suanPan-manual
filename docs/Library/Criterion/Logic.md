# Logic

Multiple criteria can be combined to emit termination signal under certain logical conditions.

## Syntax

```text title="Logic"
criterion LogicAll (1) [(2)...]
criterion LogicAny (1) [(2)...]
# (1) int, unique criterion tag
# [(2)...] int, tag of criteria in logical operation
```
